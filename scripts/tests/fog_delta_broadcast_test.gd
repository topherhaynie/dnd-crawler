# Minimal manual test harness for fog delta broadcast
extends Node

func _ready() -> void:
	print("fog_delta_broadcast_test: Manual test harness.")
	print("Attach this script to a Node in a scene, open the scene in Godot, and run it.")
	print("Do NOT instantiate DMWindow automatically; follow project docs to run the manual steps.")
